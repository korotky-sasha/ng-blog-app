import { NodeHostDirective } from './node-host.directive';

describe('NodeHostDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeHostDirective();
    expect(directive).toBeTruthy();
  });
});
